NEWWEAPON -gr_movement,wbomb1.png,1219820391
//#CLIENTSIDE
if (playerenters) {
  this.loc={playerx,playery};
  this.oldloc=this.loc;
  timeout=0.05;
}
if (timeout) {
  this.loc={playerx,playery};
  if (this.loc[0] != this.oldloc[0]) setstring gr.x,#v(playerx);
  if (this.loc[1] != this.oldloc[1]) setstring gr.y,#v(playery);
  this.oldloc=this.loc;
  timeout=0.05;
}
ENDWEAPON
